import { PublishersPipe } from './publishers.pipe';

describe('PublishersPipe', () => {
  it('create an instance', () => {
    const pipe = new PublishersPipe();
    expect(pipe).toBeTruthy();
  });
});
